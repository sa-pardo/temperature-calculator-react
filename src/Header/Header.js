import React, { Component } from "react";
import "./Header.css";

export default class Header extends Component {
  render() {
    return (
      <header>
        <p className="header-title">The ° Calculator</p>
      </header>
    );
  }
}
